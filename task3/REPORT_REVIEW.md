# Краткий отчёт по review.csv

## Объём

| Метрика | Значение |
|---------|----------|
| Строк в файле | 207 |
| Полностью пустых | 2 |
| Уникальных id | 202 |
| После очистки (готовы к загрузке) | **202** |

## Состав (после нормализации)

- Большинство id серии `c_001xxx` (новые относительно page_*.json)
- 6 id уже есть в основной базе (`c_000098`, `c_000246`, …)
- 6 id серии `c_900xxx` (похоже на тестовые)
- Города: в основном те же 20, что в основной выгрузке
- Категории: пересекаются с основной таксономией

## Качество данных

Файл **сырой и грязный**. Обнаружено 12 классов аномалий
(подробно — в [ANOMALIES.md](ANOMALIES.md)):

- mojibake (2 строки)
- rating ∉ [0, 5] (−3, 7.2, «4,5»)
- reviews_count = «много» / −10 / 45.5
- города: Moscow, москва, Санкат-Петербург, адрес в поле city
- сдвиг колонок (c_001015)
- телефоны-заглушки, shared-site.ru, htp://
- дубликаты в хвосте файла

## Загрузка

```bash
python scripts/load_review.py --dry-run   # 202 строки
python scripts/load_review.py --apply     # INSERT … ON CONFLICT DO NOTHING
```

Скрипт чистит recoverable-проблемы, пропускает битые, не затирает
существующие id из page_*.json.

## Вывод

Это не «чистая свежая выгрузка», а смесь новых записей, дублей,
тестовых id и данных с ошибками выгрузки/кодировки. Перед продом
обязателен проход через load_review.py (или аналог) + ручной разбор
ANOMALIES.md.
