# Copy to .env.local and fill with real values.
# Never commit .env.local or real secrets.

# PostgreSQL connection (from Task 1)
DATABASE_URL=postgresql://company_loader:loader_pass@localhost:5432/companies

# Alternative individual vars (if not using DATABASE_URL)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=companies
# PGUSER=company_loader
# PGPASSWORD=loader_pass
